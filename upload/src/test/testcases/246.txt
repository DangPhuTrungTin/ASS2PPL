
var a, b, c: real;

var x, y, z: Boolean;
    g, h, y: Integer;

function nty(): Real;
var x, y, z: Integer;
begin
    readLine();
    // This is readLine()
    INthis := readStdin();   
    with i: integer; do begin
        for ID := 4 downto -5 do hello := 6;
        if i > 6 then return;
    end

    return 1;
end

var q, w : integer;

function hgt(): Boolean;
begin
    (*
        =======================================
        Comment here
        =======================================
        begin
    *)
end
